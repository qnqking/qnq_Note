# 架构与安全

---

## 一、设计模式

常用 4 种：

### 单例模式（Singleton）

保证一个类仅有一个实例。饿汉模式（天生线程安全）、懒汉模式（DCL 双重检查锁）。

### 工厂模式（Factory）

定义创建对象的接口，让子类决定实例化哪个类。

### 代理模式（Proxy）

- JDK 代理：基于接口
- CGLIB 代理：基于子类继承

### 适配器模式（Adapter）

将不兼容接口转换为客户端期望的接口。

---

## 二、Maven

Java 项目的主流构建和依赖管理工具。

### pom.xml

核心配置文件，定义坐标（groupId、artifactId、version）、依赖、插件。

### 生命周期

```
clean → compile → test → package → install → deploy
```

### 常用命令

```bash
mvn clean           # 清理编译产物
mvn compile         # 编译源码
mvn test            # 运行测试
mvn package         # 打包（jar/war）
mvn install         # 安装到本地仓库
mvn dependency:tree # 查看依赖树
```

### 依赖范围（scope）

- `compile`（默认）：编译、测试、运行
- `provided`：运行时由容器提供
- `runtime`：运行时需要
- `test`：仅测试时需要

### Gradle 对比

Gradle 基于 Groovy/Kotlin DSL，构建脚本更灵活，增量构建性能更好，Android 项目默认使用。

---

## 三、REST API 设计

### HTTP 方法语义

| 方法 | 操作 | 特点 |
|------|------|------|
| GET | 查询 | 幂等、安全 |
| POST | 创建 | 非幂等 |
| PUT | 全量更新 | 幂等 |
| PATCH | 部分更新 | — |
| DELETE | 删除 | 幂等 |

### 状态码规范

200 成功 / 201 创建成功 / 204 删除成功 / 400 参数错误 / 401 未认证 / 403 无权限 / 404 资源不存在 / 500 服务器错误

### URL 设计

资源名用复数（`/api/users`），层级关系清晰（`/api/users/{id}/orders`）。

### 统一返回格式

```json
{"code": 200, "data": {...}, "message": "ok"}
```

### 分页

`/api/users?page=1&size=20`

---

## 四、安全与认证

### JWT（JSON Web Token）

- 结构：Header.Payload.Signature（三部分 Base64 编码）
- 无状态，适合分布式系统
- Access Token（短期）+ Refresh Token（长期）

### OAuth2

授权码模式（最安全）、密码模式、客户端模式、简化模式。第三方登录的核心协议。

### Spring Security

- 过滤器链 + 认证（Authentication）+ 授权（Authorization）
- `SecurityFilterChain` 配置 URL 权限
- `PasswordEncoder`（推荐 BCrypt）
- `@PreAuthorize` 方法级权限控制

### 常见攻击防护

| 攻击 | 防御 |
|------|------|
| SQL 注入 | 参数化查询（MyBatis `#{}`） |
| XSS | 对输出进行 HTML 转义 |
| CSRF | Spring Security 默认开启 CSRF 保护 |
| CORS | 配置跨域白名单 |

---

## 五、服务器与部署

### Tomcat

Web 服务器，Servlet 容器。接收用户请求、运行 Java 后台程序。

### Nginx

高性能 HTTP 服务器，六大作用：
1. Web 程序服务器（前端代码）
2. 反向代理
3. 内容缓存
4. 负载均衡
5. TCP/UDP 代理
6. 邮件代理

### 正向代理 vs 反向代理

- **正向代理**：从内向外代理（代理客户端）
- **反向代理**：从外向内代理（代理服务端，隐藏内部细节）

### 负载均衡算法

轮询、随机、最少时间、最少连接、IPHash、权重

### Docker

容器化技术，打包应用及依赖。

### 微服务

Spring Cloud，服务拆分，分布式事务。

---

## 六、对象存储（OSS）

存储非结构化数据（图片/视频/音频/文档）。

平台：阿里云 OSS、腾讯云 OSS、七牛云、MinIO（本地）。

结构化数据 → MySQL，非结构化 → OSS。

---

## 七、单元测试

### JUnit 5

```java
@Test
void testAddition() {
    assertEquals(4, 2 + 2);
}
```

常用注解：`@Test`、`@BeforeEach`、`@AfterEach`、`@DisplayName`、`@ParameterizedTest`

### Mockito

```java
@Mock private UserRepository userRepo;
@InjectMocks private UserService userService;

@Test
void testFindUser() {
    when(userRepo.findById(1L)).thenReturn(Optional.of(new User()));
    User user = userService.getUser(1L);
    assertNotNull(user);
    verify(userRepo).findById(1L);
}
```

覆盖率：建议核心业务逻辑达到 80% 以上。

---

## 相关笔记

- [[JAVA/Spring-IoC-DI]] — 单例模式、工厂模式在 Spring 中的实现
- [[JAVA/Spring-AOP]] — 代理模式的应用
- [[JAVA/Spring-Boot]] — 自动配置与 Starter
- [[Docker]] — 容器化部署
- [[Redis]] — 缓存中间件
- [[计算机网络]] — HTTP、HTTPS
